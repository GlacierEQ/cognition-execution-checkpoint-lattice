[project]
name = "cognition-execution-checkpoint-lattice"
version = "0.0.1"
description = "Scaffold: Execution Checkpoint Lattice (independent Cognition lens)"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
